{
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile.backend",
    "buildArgs": {
      "SERVICE": "gateway-service"
    }
  },
  "deploy": {
    "startCommand": "./gateway-service",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE"
  }
}
